import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import RoutingConfiguration from './pages/RoutingConfiguration';

ReactDOM.render(
  <React.StrictMode>
    <RoutingConfiguration />
  </React.StrictMode>,
  document.getElementById('root')
);
